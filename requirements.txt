# ── Core ─────────────────────────────────────────────────────────────
customtkinter>=5.2.0
Pillow>=10.0.0
pillow-avif-plugin>=1.4.0
piexif>=1.1.3
darkdetect>=0.8.0
python-dotenv>=1.0.0


# ── Drag & Drop ──────────────────────────────────────────────────────
# Windows / Linux: standard tkinterdnd2 includes pre-built tkdnd binaries
tkinterdnd2>=0.3.0; sys_platform != "darwin"

# macOS: use the universal2 fork that supports both Intel and Apple Silicon (M1/M2/M3)
# Install manually if on macOS:
#   pip install tkinterdnd2-universal
# tkinterdnd2-universal>=0.3.0; sys_platform == "darwin"

# ── Packaging (optional, install when building executables) ──────────
# pyinstaller>=6.0
